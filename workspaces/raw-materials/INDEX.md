# 原始资料 (Raw Materials) — 共享 · 只读

存放与用户工作内容相关的原始资料，**所有会话/代理共享**。

规则：
- **只读参考，禁止修改**（agent 不得在此写入或编辑）。
- 用户在对话中上传的文件落在 `uploads/<sessionId>/` 下。
- 收到重要资料时，应判断是否将要点用 `wiki_write` 总结进 AI Wiki（务必带来源路径）。
