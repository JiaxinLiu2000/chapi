/**
 * Single source of truth for the app version, shown at the bottom of the web UI
 * and included in each release commit (vX.Y.Z).
 *
 * Convention: bump the PATCH (third) digit on every code update, and use the
 * same vX.Y.Z in the commit message.
 */
export const APP_VERSION = '0.1.44';
